# Code location: load definitions from repo root (/app in Docker).
# python_module + working_directory is reliable when workspace.yaml lives in
# orchestration/dagster (different dir from definitions.py).
load_from:
  - python_module:
      module_name: definitions
      working_directory: /app
      attribute: defs

