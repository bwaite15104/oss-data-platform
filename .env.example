# Cursor Agent CLI (ml_iteration/run.py). Get key: Cursor → Integrations → User API Keys.
CURSOR_API_KEY=

# Optional (default: http://localhost:5000)
# MLFLOW_TRACKING_URI=http://localhost:5000

# -----------------------------------------------------------------------------
# Postgres (used by scripts, Dagster, and dlt ingestion)
# -----------------------------------------------------------------------------
# Copy to .env and set values. Required for: load_raw_tables.py, Dagster assets,
# SQLMesh, and dlt pipelines. When these are set, dlt automatically gets
# Postgres credentials for the ingestion assets (no separate dlt vars needed).
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=nba_analytics
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Dagster instance storage (dagster.yaml). For local "dagster dev" set these to
# match POSTGRES_* above. In Docker they are set by docker-compose (host=postgres).
DAGSTER_DATABASE_HOST=localhost
DAGSTER_DATABASE_PORT=5432
DAGSTER_DATABASE=nba_analytics
DAGSTER_DATABASE_USER=postgres
DAGSTER_DATABASE_PASSWORD=postgres

# Environment for raw/staging/marts schema suffix (dev, staging, prod)
DATA_ENV=dev

# Optional: directory for load_raw_tables.py (e.g. CSV export from export_raw_tables.py)
# RAW_EXPORT_DATA_DIR=../Code Data/raw_dev_export
